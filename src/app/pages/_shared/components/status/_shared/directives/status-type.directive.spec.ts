import { StatusTypeDirective } from './status-type.directive';

describe('StatusTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusTypeDirective();
    expect(directive).toBeTruthy();
  });
});
